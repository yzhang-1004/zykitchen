# 🗄️ ZY厨房 - 微信云开发数据库持久化方案

## 📋 文档信息
- **版本**: v1.0
- **创建时间**: 2026-06-08
- **作者**: ZY厨房开发团队
- **状态**: 📝 待评审

---

## 🎯 1. 方案概述

### 1.1 背景
当前项目所有数据（菜品、购物车、待做订单、烹饪记录）均存储在 `app.globalData`（内存）中，存在以下问题：
- ❌ 删除小程序后数据丢失
- ❌ 关闭小程序后数据清空
- ❌ 不同设备间数据无法同步
- ❌ 无法进行数据备份和恢复

### 1.2 目标
使用**微信云开发数据库**实现数据持久化，确保：
- ✅ 数据永久保存
- ✅ 多设备数据同步
- ✅ 支持离线访问
- ✅ 保证数据安全性

### 1.3 技术选型
- **数据库**: 微信云开发数据库（NoSQL）
- **存储**: 微信云开发云存储（图片）
- **前端**: 微信小程序原生开发

---

## 🏗️ 2. 数据库设计

### 2.1 集合（Collections）设计

#### 📦 集合1: `dishes` - 菜品信息
存储所有用户创建的菜品数据。

**字段定义：**
```javascript
{
  _id: "自动生成",           // 云数据库自动生成的唯一ID
  _openid: "自动生成",       // 用户的openid（云开发自动注入）
  
  // 基本信息
  id: 1234567890,           // 菜品ID（时间戳，用于前端快速查找）
  name: "红烧肉",           // 菜品名称（必填）
  category: "meat",         // 分类：meat/vegetable/noodle
  categoryName: "肉菜",     // 分类中文名
  
  // 详细信息
  description: "美味的红烧肉",  // 菜品描述
  images: [                     // 菜品图片（最多3张）
    "cloud://xxx.jpg",
    "cloud://yyy.jpg"
  ],
  
  // 菜谱信息
  recipe: "1. 切肉\n2. 焯水\n3. 炖煮",  // 菜谱步骤（文本）
  recipeImages: [                          // 菜谱图片（最多3张）
    "cloud://aaa.jpg",
    "cloud://bbb.jpg"
  ],
  
  // 统计数据
  cookCount: 5,             // 被做次数
  createTime: "2026-06-08T10:00:00.000Z",  // 创建时间
  updateTime: "2026-06-08T12:00:00.000Z"   // 更新时间
}
```

**索引设计：**
- `_id`: 主键索引（自动）
- `_openid`: 普通索引（按用户查询）
- `category`: 普通索引（按分类查询）
- `createTime`: 普通索引（按时间排序）

---

#### 🛒 集合2: `cart_items` - 购物车
存储用户当前的购物车数据。

**字段定义：**
```javascript
{
  _id: "自动生成",
  _openid: "自动生成",       // 用户标识
  
  items: [                   // 购物车项列表
    {
      dishId: 1234567890,    // 菜品ID
      dishName: "红烧肉",    // 菜品名称（冗余，提高查询效率）
      dish: {                // 完整菜品对象（冗余）
        id: 1234567890,
        name: "红烧肉",
        // ... 其他字段
      },
      quantity: 2            // 数量
    }
  ],
  
  updateTime: "2026-06-08T12:00:00.000Z"
}
```

**设计说明：**
- 每个用户只有一个购物车文档
- 使用 `_openid` 唯一标识用户
- 每次更新购物车时覆盖整个文档

---

#### 📝 集合3: `todo_orders` - 待做订单
存储用户下单后待做的菜品列表。

**字段定义：**
```javascript
{
  _id: "自动生成",
  _openid: "自动生成",
  
  id: 1234567890.123,       // 订单项ID（时间戳+随机数）
  dishId: 1234567890,       // 菜品ID
  dishName: "红烧肉",       // 菜品名称
  dish: {                   // 完整菜品对象（冗余）
    // ... 菜品完整信息
  },
  quantity: 2,              // 数量
  
  // 菜谱步骤（预处理后的数组）
  recipeSteps: [
    "1. 切肉",
    "2. 焯水",
    "3. 炖煮"
  ],
  
  // 时间信息
  time: "2026/6/8 12:00:00",  // 下单时间（中文格式）
  createTime: "2026-06-08T12:00:00.000Z",
  
  // 状态
  status: "pending"         // pending: 待做 / cooking: 烹饪中 / done: 已完成
}
```

**索引设计：**
- `_id`: 主键索引
- `_openid`: 普通索引
- `status`: 普通索引（筛选待做订单）
- `createTime`: 普通索引（按时间排序）

---

#### ⭐ 集合4: `user_stats` - 用户统计
存储用户的统计数据。

**字段定义：**
```javascript
{
  _id: "自动生成",
  _openid: "自动生成",
  
  starCount: 10,            // 星星总数
  totalCookCount: 50,       // 总烹饪次数
  
  // 烹饪历史记录
  cookHistory: [
    {
      dishId: 1234567890,
      dishName: "红烧肉",
      time: "2026/6/8 12:00:00",
      stars: 1
    }
    // ... 更多记录（最近100条）
  ],
  
  updateTime: "2026-06-08T12:00:00.000Z"
}
```

**设计说明：**
- 每个用户只有一个统计文档
- `cookHistory` 数组最多保留100条（FIFO）
- 超出100条时，删除最早的记录

---

#### 🖼️ 集合5: `files` - 云存储文件记录
记录所有上传到云存储的图片文件。

**字段定义：**
```javascript
{
  _id: "自动生成",
  _openid: "自动生成",
  
  fileID: "cloud://xxx.jpg",    // 云存储文件ID
  fileName: "dish_123456.jpg",  // 文件名
  fileType: "dish",             // 类型：dish/recipe
  relatedDishId: 1234567890,    // 关联的菜品ID
  
  size: 102400,                 // 文件大小（字节）
  createTime: "2026-06-08T12:00:00.000Z"
}
```

---

## 🔌 3. 云开发配置

### 3.1 初始化云开发环境

**修改 `app.js`：**
```javascript
App({
  onLaunch() {
    // 初始化云开发环境
    if (!wx.cloud) {
      console.error('请使用 2.2.3 或以上的基础库以使用云能力');
    } else {
      wx.cloud.init({
        env: 'your-env-id',  // 云环境ID（在云开发控制台获取）
        traceUser: true      // 是否在将用户访问记录到用户管理中
      });
    }
    
    // ... 其他初始化代码
  }
})
```

### 3.2 修改 `project.config.json`

添加云开发配置：
```json
{
  "cloudfunctionRoot": "cloudfunctions/",
  "miniprogramRoot": "./",
  "setting": {
    "urlCheck": true,
    "es6": true,
    // ... 其他配置
  },
  "appid": "wx62a73bdd1187d57a"
}
```

### 3.3 云开发控制台设置

**需要在微信开发者工具中操作：**

1. **开通云开发**
   - 点击工具栏"云开发"按钮
   - 创建环境（建议选择"按量计费"）
   - 记录环境ID（如：`kawaii-shop-xxxxx`）

2. **创建集合**
   - 进入"数据库"
   - 创建以下集合：
     - `dishes`
     - `cart_items`
     - `todo_orders`
     - `user_stats`
     - `files`

3. **设置权限**
   - 所有集合权限设置为：**仅创建者可读写**
   - 确保用户只能访问自己的数据

4. **添加索引**（可选，优化性能）
   - 为常用查询字段添加索引

---

## 💻 4. 核心代码实现

### 4.1 数据库操作工具类

**新建 `utils/db.js`：**

```javascript
/**
 * 云数据库操作工具类
 */
class DB {
  constructor() {
    this.db = wx.cloud.database();
    this._ = this.db.command;
  }

  /**
   * 获取集合引用
   * @param {string} name 集合名称
   */
  collection(name) {
    return this.db.collection(name);
  }

  /**
   * 添加文档
   * @param {string} collection 集合名称
   * @param {object} data 数据
   */
  async add(collection, data) {
    try {
      const now = new Date().toISOString();
      const result = await this.collection(collection).add({
        data: {
          ...data,
          createTime: now,
          updateTime: now
        }
      });
      return { success: true, data: result };
    } catch (err) {
      console.error('添加文档失败:', err);
      return { success: false, error: err };
    }
  }

  /**
   * 查询文档
   * @param {string} collection 集合名称
   * @param {object} where 查询条件
   * @param {number} limit 限制数量
   */
  async get(collection, where = {}, limit = 100) {
    try {
      const result = await this.collection(collection)
        .where(where)
        .limit(limit)
        .orderBy('createTime', 'desc')
        .get();
      return { success: true, data: result.data };
    } catch (err) {
      console.error('查询文档失败:', err);
      return { success: false, error: err };
    }
  }

  /**
   * 更新文档
   * @param {string} collection 集合名称
   * @param {string} id 文档ID
   * @param {object} data 更新的数据
   */
  async update(collection, id, data) {
    try {
      const result = await this.collection(collection).doc(id).update({
        data: {
          ...data,
          updateTime: new Date().toISOString()
        }
      });
      return { success: true, data: result };
    } catch (err) {
      console.error('更新文档失败:', err);
      return { success: false, error: err };
    }
  }

  /**
   * 删除文档
   * @param {string} collection 集合名称
   * @param {string} id 文档ID
   */
  async remove(collection, id) {
    try {
      const result = await this.collection(collection).doc(id).remove();
      return { success: true, data: result };
    } catch (err) {
      console.error('删除文档失败:', err);
      return { success: false, error: err };
    }
  }

  /**
   * 获取当前用户的openid
   */
  async getOpenid() {
    try {
      const { result } = await wx.cloud.callFunction({
        name: 'login'
      });
      return result.openid;
    } catch (err) {
      console.error('获取openid失败:', err);
      return null;
    }
  }
}

module.exports = new DB();
```

---

### 4.2 云函数：获取用户openid

**新建 `cloudfunctions/login/index.js`：**

```javascript
// 云函数入口文件
const cloud = require('wx-server-sdk');

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
});

// 云函数入口函数
exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext();
  
  return {
    openid: wxContext.OPENID,
    appid: wxContext.APPID,
    unionid: wxContext.UNIONID
  };
};
```

**新建 `cloudfunctions/login/package.json`：**
```json
{
  "name": "login",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~2.6.3"
  }
}
```

---

### 4.3 图片上传到云存储

**新建 `utils/upload.js`：**

```javascript
/**
 * 上传图片到云存储
 * @param {string} filePath 本地文件路径
 * @param {string} folder 文件夹名称（dish/recipe）
 * @returns {Promise} 云存储文件ID
 */
async function uploadImage(filePath, folder = 'dish') {
  try {
    const fileName = `${folder}_${Date.now()}_${Math.random().toString(36).substr(2, 9)}.jpg`;
    const cloudPath = `${folder}/${fileName}`;
    
    const result = await wx.cloud.uploadFile({
      cloudPath: cloudPath,
      filePath: filePath
    });
    
    console.log('上传成功:', result.fileID);
    return result.fileID;
  } catch (err) {
    console.error('上传失败:', err);
    throw err;
  }
}

/**
 * 批量上传图片
 * @param {Array} filePaths 本地文件路径数组
 * @param {string} folder 文件夹名称
 * @returns {Promise<Array>} 云存储文件ID数组
 */
async function uploadImages(filePaths, folder = 'dish') {
  const uploadPromises = filePaths.map(path => uploadImage(path, folder));
  return Promise.all(uploadPromises);
}

/**
 * 删除云存储文件
 * @param {string} fileID 文件ID
 */
async function deleteImage(fileID) {
  try {
    await wx.cloud.deleteFile({
      fileList: [fileID]
    });
    console.log('删除成功:', fileID);
  } catch (err) {
    console.error('删除失败:', err);
    throw err;
  }
}

module.exports = {
  uploadImage,
  uploadImages,
  deleteImage
};
```

---

## 🔄 5. 数据同步策略

### 5.1 启动时加载数据

**修改 `app.js` 的 `onLaunch`：**

```javascript
async onLaunch() {
  // ... 云开发初始化
  
  // 加载用户数据
  await this.loadUserData();
},

async loadUserData() {
  const db = require('./utils/db');
  
  // 显示加载提示
  wx.showLoading({ title: '加载数据中...', icon: 'none' });
  
  try {
    // 并行加载所有数据
    const [dishesRes, cartRes, todoRes, statsRes] = await Promise.all([
      db.get('dishes'),
      db.get('cart_items'),
      db.get('todo_orders', { status: 'pending' }),
      db.get('user_stats')
    ]);
    
    // 更新全局数据
    if (dishesRes.success) {
      this.globalData.dishes = dishesRes.data;
    }
    
    if (cartRes.success && cartRes.data.length > 0) {
      this.globalData.cartItems = cartRes.data[0].items || [];
    }
    
    if (todoRes.success) {
      this.globalData.todoOrders = todoRes.data;
    }
    
    if (statsRes.success && statsRes.data.length > 0) {
      this.globalData.starCount = statsRes.data[0].starCount || 0;
      this.globalData.cookHistory = statsRes.data[0].cookHistory || [];
    }
    
    console.log('数据加载完成');
  } catch (err) {
    console.error('加载数据失败:', err);
    wx.showToast({
      title: '数据加载失败',
      icon: 'none'
    });
  } finally {
    wx.hideLoading();
  }
}
```

---

### 5.2 数据变更时同步到云端

**核心原则：每次修改数据后，立即同步到云数据库**

#### 示例：添加菜品

**修改 `pages/manage/manage.js` 的 `submitDish` 方法：**

```javascript
async submitDish() {
  const { name, category, categoryName } = this.data.formData;
  
  // 表单验证
  if (!name.trim()) {
    wx.showToast({ title: '请输入菜品名称', icon: 'none' });
    return;
  }
  
  wx.showLoading({ title: '保存中...', icon: 'none' });
  
  try {
    const db = require('../../utils/db');
    const upload = require('../../utils/upload');
    
    // 1. 上传图片到云存储
    let images = [];
    if (this.data.formData.images.length > 0) {
      images = await upload.uploadImages(this.data.formData.images, 'dish');
    }
    
    let recipeImages = [];
    if (this.data.formData.recipeImages.length > 0) {
      recipeImages = await upload.uploadImages(this.data.formData.recipeImages, 'recipe');
    }
    
    // 2. 准备数据
    const dishData = {
      id: Date.now(),
      name: name,
      category: category,
      categoryName: categoryName,
      description: this.data.formData.description,
      images: images,
      recipe: this.data.formData.recipe,
      recipeImages: recipeImages,
      cookCount: 0
    };
    
    // 3. 添加到云数据库
    const result = await db.add('dishes', dishData);
    
    if (result.success) {
      // 4. 更新本地数据
      const dishes = this.data.allDishes;
      dishes.push({
        ...dishData,
        _id: result.data._id  // 保存云数据库的ID
      });
      app.globalData.dishes = dishes;
      
      wx.showToast({
        title: '添加成功 🎉',
        icon: 'success'
      });
      
      this.setData({ showModal: false });
      this.loadDishes();
    } else {
      throw result.error;
    }
  } catch (err) {
    console.error('保存菜品失败:', err);
    wx.showToast({
      title: '保存失败，请重试',
      icon: 'none'
    });
  } finally {
    wx.hideLoading();
  }
}
```

---

### 5.3 购物车同步

**修改 `pages/order/order.js`：**

```javascript
async addToCart(e) {
  const dish = e.currentTarget.dataset.dish;
  const cartItems = app.globalData.cartItems || [];
  
  // 检查是否已存在
  const existIndex = cartItems.findIndex(item => item.dish.id === dish.id);
  if (existIndex !== -1) {
    cartItems[existIndex].quantity += 1;
  } else {
    cartItems.push({ dish: dish, quantity: 1 });
  }
  
  app.globalData.cartItems = cartItems;
  this.updateCartCount();
  
  wx.showToast({ title: '已加入点菜篮 🧺', icon: 'none' });
  
  // 同步到云数据库（节流：300ms内只同步一次）
  this._syncCartToCloud();
},

// 节流同步购物车
_syncCartToCloud: _.throttle(async function() {
  const db = require('../../utils/db');
  const cartItems = app.globalData.cartItems;
  
  try {
    // 查询是否已存在购物车文档
    const existing = await db.get('cart_items');
    
    if (existing.success && existing.data.length > 0) {
      // 更新现有购物车
      await db.update('cart_items', existing.data[0]._id, {
        items: cartItems
      });
    } else {
      // 创建新购物车
      await db.add('cart_items', {
        items: cartItems
      });
    }
    
    console.log('购物车已同步到云端');
  } catch (err) {
    console.error('同步购物车失败:', err);
  }
}, 300)
```

---

## 🔒 6. 安全性考虑

### 6.1 数据权限

- **集合权限**：设置为"仅创建者可读写"
- **用户隔离**：通过 `_openid` 自动隔离不同用户的数据
- **敏感数据**：不在前端存储敏感信息

### 6.2 数据校验

- **前端校验**：表单验证、数据类型检查
- **云函数校验**：在云函数中二次验证（可选）

### 6.3 防止数据丢失

- **自动保存**：每次操作后立即同步到云端
- **离线缓存**：使用 `wx.setStorageSync` 作为临时缓存
- **错误处理**：网络失败时提示用户重试

---

## 💰 7. 成本预估

### 7.1 免费额度（按量计费环境）

| 资源 | 免费额度 | 超出后费用 |
|------|---------|-----------|
| 数据库存储 | 2GB | 0.1元/GB/天 |
| 数据库读次数 | 5万次/天 | 0.1元/万次 |
| 数据库写次数 | 1万次/天 | 0.1元/万次 |
| 云存储容量 | 5GB | 0.03元/GB/天 |
| 云存储下载流量 | 5GB/月 | 0.5元/GB |
| 云函数调用 | 10万次/月 | 0.5元/万次 |

### 7.2 预估使用量（个人使用）

| 指标 | 预估用量 | 是否在免费额度内 |
|------|---------|----------------|
| 数据库存储 | < 100MB | ✅ 是 |
| 每日读次数 | < 1000次 | ✅ 是 |
| 每日写次数 | < 200次 | ✅ 是 |
| 云存储容量 | < 500MB | ✅ 是 |
| 云存储流量 | < 1GB/月 | ✅ 是 |

**结论：个人使用完全免费！** 🎉

---

## 🚀 8. 实施步骤

### 阶段1：准备工作（预计10分钟）
- [ ] 1. 在微信开发者工具中开通云开发
- [ ] 2. 创建云环境（记录环境ID）
- [ ] 3. 创建5个数据库集合
- [ ] 4. 设置集合权限

### 阶段2：代码修改（预计1小时）
- [ ] 5. 创建 `utils/db.js` 数据库工具类
- [ ] 6. 创建 `utils/upload.js` 上传工具类
- [ ] 7. 创建 `cloudfunctions/login` 云函数
- [ ] 8. 修改 `app.js` 初始化云开发
- [ ] 9. 修改 `project.config.json` 添加云函数目录

### 阶段3：功能改造（预计2小时）
- [ ] 10. 改造 `pages/manage/manage.js`（菜品CRUD）
- [ ] 11. 改造 `pages/order/order.js`（购物车和下单）
- [ ] 12. 改造 `pages/profile/profile.js`（统计数据）
- [ ] 13. 添加图片上传功能
- [ ] 14. 添加数据迁移功能

### 阶段4：测试验证（预计30分钟）
- [ ] 15. 测试添加菜品
- [ ] 16. 测试购物车同步
- [ ] 17. 测试下单功能
- [ ] 18. 测试多设备数据同步
- [ ] 19. 测试删除小程序后数据恢复

### 阶段5：上线部署（预计10分钟）
- [ ] 20. 上传代码
- [ ] 21. 设置为体验版测试
- [ ] 22. 提交审核

**总耗时预估：4小时**

---

## ⚠️ 9. 注意事项

### 9.1 开发环境
- 基础库版本 ≥ 2.2.3
- 微信开发者工具最新版本
- 确保已开通云开发

### 9.2 数据一致性
- 每次操作后立即同步到云端
- 使用 `wx.showLoading` 提示用户正在保存
- 网络失败时提示用户重试

### 9.3 性能优化
- 使用数据库索引优化查询
- 购物车同步使用节流（300ms）
- 图片上传使用压缩

### 9.4 用户体验
- 启动时显示"加载数据中"提示
- 操作时显示"保存中"提示
- 失败时给出明确错误信息

---

## 📝 10. 后续扩展

### 10.1 高级功能（可选）
- 数据导出（导出为JSON/Excel）
- 数据分享（分享给好友）
- 数据统计（烹饪趋势图表）
- 多用户协作（家庭共享菜单）

### 10.2 性能优化（可选）
- 数据分页加载
- 图片压缩和CDN加速
- 离线模式支持
- 数据缓存策略

---

## ✅ 11. 验收标准

- [ ] 数据持久化：删除小程序后数据不丢失
- [ ] 多设备同步：在不同手机上数据一致
- [ ] 图片上传：图片成功上传到云存储
- [ ] 离线可用：无网络时仍可查看已加载数据
- [ ] 错误处理：网络失败时有友好提示
- [ ] 性能良好：操作流畅，无明显卡顿

---

## 📞 12. 技术支持

如有问题，请联系开发团队或在微信开发者工具中查看云开发文档。

---

**文档版本**: v1.0  
**最后更新**: 2026-06-08  
**状态**: 📝 待评审

**请评审此方案，确认后我将开始实施代码改造！** 🚀